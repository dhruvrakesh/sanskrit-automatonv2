fastapi==0.112.2
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9
httpx==0.27.2
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
regex==2024.9.11
indic-transliteration==2.3.55
# sanskrit_parser==0.2.6  <-- REMOVED: its werkzeug==2.1.2 pin conflicts with
#   flask>=3.0.3 (needs werkzeug>=3.1). It is pre-installed system-wide and
#   not imported directly by any pipeline script. Install manually if needed:
#   pip install sanskrit_parser==0.2.6 --no-deps
networkx==3.3
rapidfuzz==3.9.7
pandas==2.2.3
jinja2==3.1.4
pdfminer.six
pdf2image
pytesseract
ocrmypdf
watchdog
pypdf

# ── Dashboard (Flask) ──────────────────────────────
werkzeug>=3.1.0
flask>=3.0.3

# ── Translation APIs ────────────────────────────────
openai>=1.40.0
google-generativeai>=0.8.0

# ── Env & config ────────────────────────────────────
python-dotenv>=1.0.1

# ── OCR preprocessing (optional but recommended) ────
opencv-python-headless>=4.10.0.84

# ── PDF export (optional -- for server-side PDF) ────
# playwright  # uncomment and run: playwright install chromium
