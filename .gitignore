# DB backups (keep locally, don’t commit)
data/*_backup_*.db
*.db-journal
*.db-wal
*.db-shm

# Script backup / archives
scripts/scripts.zip

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.local
.env.*.local

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Main DB (recreate locally; don’t commit)
data/context.db

# Runtime state files (generated by pipeline; never commit)
data/translation_progress.json
data/translation_config.json
data/jobs.jsonl

# Scratch / one-off scripts (already tracked, but don't track new ones)
tmp_*.py
tmp*.txt
AUDIT_BUGS_AND_CLEANUP.md

# Large export artefacts (regenerate from DB)
exports/*.html
exports/*.tsv
out/

data/raw/
exports/
inbox/
out/
__pycache__/
.fuse_hidden*

data/raw/
exports/
inbox/
out/
__pycache__/
.fuse_hidden*

query_snapshot.db
query_snapshot.db-*


# ---- added 2026-08-30: OCR working directories and local backups ----
# backups/ reached 523 MB; data/raw_vision holds one JSONL per page per book.
# None of it is source - all of it is regenerable from inbox/ plus the scripts.
backups/
data/raw_vision/
data/raw_merged/
data/probe/
data/manifests/
data/vision_redo.txt
data/tmp_*.jsonl

# ...but DO keep the probe verdicts: they are small, and they are the evidence
# behind the OCR standard in RUNBOOK 3d and BENCHMARKS.
!data/ocr_probe_results.json
!data/ocr_probe_results_dense.json
!data/ocr_probe_results_v1_evenspaced.json

# One-off release message drafts (use scripts\release.ps1 -MessageFile)
COMMIT_MSG.txt
